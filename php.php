<?php

if (isset($_POST["dicom-file"]) && isset($_POST["report"])) {
  $dicomFile = $_POST["dicom-file"];
  $report = $_POST["report"];

  // Add the report to the DICOM file.

  // Save the DICOM file.
}

?>
